spring:
  application:
    name: begin-web
  cloud:
    nacos:
      discovery:
        server-addr: nacos:8848
      config:
        server-addr: nacos:8848

logging:
  config: classpath:logback-spring.xml
  path: D:/logs/begin-web